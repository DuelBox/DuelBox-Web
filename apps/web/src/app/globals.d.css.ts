// Next.js loads this global stylesheet for its side effects. TypeScript 6 checks that the
// import resolves; keep a declaration beside the one CSS file instead of accepting every
// `*.css` spelling through a wildcard ambient module.
export {};
